#pragma once

#include <map>
#include <cstring>
#include <iostream>
#include <vector>

using namespace std;

extern map<string, string> subjectsInterpretations;
extern vector <vector <string>> TermSubjects;
extern vector<string> all_faculties;